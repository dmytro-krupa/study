package lec15;

public class ETC {
    //POJO, JavaBean, DTO, Entity, ValueObject


    //javabean - mutable, public no-args constructor, no logic, serialized, equals(), hashCode() and toString().

    /*
        1. fields should be final
        2. no setters
        3. fields should be public == no direct access
        4. initializing using constructor and using deep copy for objects
        5. each getter should return copy and not exact reference
     */
}
